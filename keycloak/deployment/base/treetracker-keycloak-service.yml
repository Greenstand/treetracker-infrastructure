apiVersion: v1
kind: Service
metadata:
  name: treetracker-keycloak-service
  annotations:
  labels:
    app: treetracker-keycloak
    name: treetracker-keycloak-service
  namespace: keycloak
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: treetracker-keycloak
