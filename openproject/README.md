# To deploy open project under /openproject/

# Not using Kustomize, and sealed secrets, so please: change the settings for 
host, and install secret for db manually.

# Install openproject

Simply apply this deployment.yaml
